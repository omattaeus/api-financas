spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/api-financas?useTimezone=true&serverTimezone=UTC
    username: root
    password: 12345678@Ta
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
spring-doc:
  pathsToMatch: /api/**
  swagger-ui:
    use-root-path: true